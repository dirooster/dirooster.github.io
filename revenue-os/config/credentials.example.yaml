# Copy to credentials.local.yaml if needed. Do not commit real secrets.

email:
  provider: yandex_smtp
  account: tech.it.rooster@yandex.ru
  smtp_host: smtp.yandex.com
  smtp_port: 465
  username_env_var: SMTP_USERNAME
  app_password_env_var: SMTP_PASSWORD
  authenticated: false

calendar:
  provider:
  authenticated: false

hosting:
  provider:
  project:
  authenticated: false

analytics:
  provider:
  property:
  authenticated: false

domains:
  registrar:
  domains: []
  authenticated: false

llm:
  provider:
  key_env_var:
